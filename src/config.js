// 配置项

// const host = 'http://localhost:5757'
// 生产环境
const host = 'https://umxmqork.qcloud.la'

const config = {
  host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}
export default config
